apiVersion: v1
kind: Service
metadata:
  name: podinfo
  namespace: {{ .Values.namespace }}
  labels:
    app: podinfo
spec:
  selector:
    app: podinfo
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  type: {{ .Values.service.type }}
